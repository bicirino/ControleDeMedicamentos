# Render.com Deploy Configuration
# Arquivo de configuração para deploy automático no Render.com

services:
  - type: web
    name: controle-medicamentos
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
