# Render.com Deploy Configuration
# Arquivo de configuração para deploy automático no Render.com

services:
  - type: web
    name: controle-medicamentos
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: DATABASE_URL
        value: ""  # Obrigatorio no Render: configure com a URL do Supabase/Neon
      - key: GROQ_API_KEY
        value: ""  # Será configurado nas variáveis de ambiente do Render
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.12"
